name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    uses: ./.github/workflows/build.yml

  sonar:
    uses: ./.github/workflows/sonar-analysis.yml
    secrets:
      SECRET: ${{ secrets.SONAR_TOKEN }}